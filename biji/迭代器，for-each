迭代器，for-each
迭代器遍历理解为一个借口，就是用来实现集合的遍历,不可以直接新建：
Interator<Object> it = list.iterator();
while(it.hasNext()){
	Object a = it.next;
	System.out.print(a);
}
用for循环：
for(Interator<Object> it = list.iterator(); it.hasNext();){
	Object s = it.next();
	//处理
}
用 for-each来表示可简化为：
for(Object s : list){
	//处理s
}
补充 for-each:
数组：
for(int i = 0;i<a.length;i++){
	String s = a[i];
	//处理s
}
for-each 简化：
for(String s : a){
	//处理s
}
